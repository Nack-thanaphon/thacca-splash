/** @type {import('next-i18next').UserConfig} */
const config = {
    i18n: {
        defaultLocale: 'en',
        locales: ['en', 'th'],
    },
    defaultNS: 'common',
    localePath: './public/locales',
}

export default config; 